[package]
name = "bench"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[[bench]]
name = "engine"
harness = false

[dev-dependencies]
criterion = "0.5"
philis = { package = "api", path = "../crates/api" }
