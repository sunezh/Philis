//! Coordinate aliases for the integer geometry kernel.
//!
//! All coordinates and distances are `i64` nanometers.

/// A coordinate value in nanometers.
pub type Coord = i64;

/// A distance (always non-negative in practice) in nanometers.
pub type Distance = i64;

/// An area in square nanometers.
pub type Area = i64;
