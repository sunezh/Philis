//! Influence radius computation.
//!
//! `compute_r_max` (PLAN.md §3.5) computes the maximum distance at which a
//! shape modification can create or resolve a DRC violation, derived from a
//! `philis-tech::RuleSet`. `philis-tech` is currently an empty stub, so this
//! is deferred until `RuleSet` exists.
//!
//! TODO(philis-tech): implement `compute_r_max(rules: &RuleSet) -> i64` once
//! `philis-tech::RuleSet` is available.
